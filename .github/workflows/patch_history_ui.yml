name: Patch history feature and panel UI

on:
  workflow_dispatch:

permissions:
  contents: write

env:
  BRANCH_NAME: main

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.BRANCH_NAME }}
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply history feature and UI patch
        shell: python
        run: |
          from pathlib import Path

          Path('lib/models/message.dart').write_text(r'''enum MessageType { text, image }

class Message {
  final String id;
  final String role; // 'user', 'assistant', 'system'
  String content; // 流式更新需要可变
  final MessageType type;

  /// 兼容旧版：单张上传文件本地路径。
  final String? localFilePath;

  /// 兼容旧版：单张上传图片 base64 编码。
  final String? base64Image;

  /// 新版：多张参考图本地路径。
  final List<String> localFilePaths;

  /// 新版：多张参考图 base64 编码，用于多模态请求。
  final List<String> base64Images;

  bool isGenerating; // 是否正在处于打字机流式生成状态
  final DateTime timestamp;

  Message({
    required this.id,
    required this.role,
    required this.content,
    this.type = MessageType.text,
    this.localFilePath,
    this.base64Image,
    List<String>? localFilePaths,
    List<String>? base64Images,
    this.isGenerating = false,
    DateTime? timestamp,
  })  : localFilePaths = localFilePaths ?? (localFilePath == null ? const <String>[] : <String>[localFilePath]),
        base64Images = base64Images ?? (base64Image == null ? const <String>[] : <String>[base64Image]),
        timestamp = timestamp ?? DateTime.now();

  List<String> get effectiveLocalFilePaths {
    if (localFilePaths.isNotEmpty) return localFilePaths.where((e) => e.trim().isNotEmpty).toList();
    final single = localFilePath;
    return single == null || single.trim().isEmpty ? const <String>[] : <String>[single];
  }

  List<String> get effectiveBase64Images {
    if (base64Images.isNotEmpty) return base64Images.where((e) => e.trim().isNotEmpty).toList();
    final single = base64Image;
    return single == null || single.trim().isEmpty ? const <String>[] : <String>[single];
  }

  Map<String, dynamic> toJson() => {
        'id': id,
        'role': role,
        'content': content,
        'type': type.name,
        'localFilePath': localFilePath,
        // 历史记录不持久化 base64 图片，避免 SharedPreferences 因大图过大而写入失败。
        // 恢复历史时仍保留文字、图片 URL/data 消息和本地参考图路径。
        'base64Image': null,
        'localFilePaths': localFilePaths,
        'base64Images': const <String>[],
        'isGenerating': false,
        'timestamp': timestamp.toIso8601String(),
      };

  factory Message.fromJson(Map<String, dynamic> json) {
    List<String> stringList(Object? value) {
      if (value is List) return value.map((e) => e.toString()).where((e) => e.trim().isNotEmpty).toList();
      return const <String>[];
    }

    final rawType = json['type']?.toString() ?? 'text';
    final parsedType = MessageType.values.firstWhere(
      (e) => e.name == rawType,
      orElse: () => MessageType.text,
    );
    return Message(
      id: json['id']?.toString() ?? DateTime.now().microsecondsSinceEpoch.toString(),
      role: json['role']?.toString() ?? 'assistant',
      content: json['content']?.toString() ?? '',
      type: parsedType,
      localFilePath: json['localFilePath']?.toString(),
      base64Image: json['base64Image']?.toString(),
      localFilePaths: stringList(json['localFilePaths']),
      base64Images: stringList(json['base64Images']),
      isGenerating: json['isGenerating'] == true,
      timestamp: DateTime.tryParse(json['timestamp']?.toString() ?? '') ?? DateTime.now(),
    );
  }

  // 转换成 OpenAI API 标准的消息格式
  Map<String, dynamic> toOpenAiMap() {
    final images = effectiveBase64Images;
    if (images.isNotEmpty) {
      // OpenAI 多模态消息格式，支持多张图片。
      return {
        'role': role,
        'content': [
          {
            'type': 'text',
            'text': content.isNotEmpty ? content : (images.length > 1 ? '分析这些图片' : '分析这张图片')
          },
          ...images.map((image) => {
                'type': 'image_url',
                'image_url': {
                  'url': 'data:image/jpeg;base64,$image'
                }
              }),
        ]
      };
    }
    // 普通文本消息格式
    return {
      'role': role,
      'content': content,
    };
  }
}
''', encoding='utf-8')

          Path('lib/services/settings_service.dart').write_text(r'''import 'dart:convert';

import 'package:shared_preferences/shared_preferences.dart';

import '../models/message.dart';

class ChatHistoryEntry {
  final String id;
  final String title;
  final DateTime updatedAt;
  final List<Message> messages;

  const ChatHistoryEntry({
    required this.id,
    required this.title,
    required this.updatedAt,
    required this.messages,
  });

  Map<String, dynamic> toJson() => {
        'id': id,
        'title': title,
        'updatedAt': updatedAt.toIso8601String(),
        'messages': messages.map((m) => m.toJson()).toList(),
      };

  factory ChatHistoryEntry.fromJson(Map<String, dynamic> json) => ChatHistoryEntry(
        id: json['id']?.toString() ?? DateTime.now().microsecondsSinceEpoch.toString(),
        title: json['title']?.toString() ?? '未命名会话',
        updatedAt: DateTime.tryParse(json['updatedAt']?.toString() ?? '') ?? DateTime.now(),
        messages: (json['messages'] is List)
            ? (json['messages'] as List)
                .whereType<Map>()
                .map((e) => Message.fromJson(Map<String, dynamic>.from(e)))
                .toList()
            : const <Message>[],
      );
}

class SettingsService {
  static const String _keyApiKey = 'api_key';
  static const String _keyImageApiKey = 'image_api_key';
  static const String _keyBaseUrl = 'base_url';
  static const String _keyImageBaseUrl = 'image_base_url';
  static const String _keyChatModel = 'chat_model';
  static const String _keyImageModel = 'image_model';
  static const String _keyImageEditModel = 'image_edit_model';
  static const String _keyImageAspectRatio = 'image_aspect_ratio';
  static const String _keyImageQuality = 'image_quality';
  static const String _keyEnhanceImagePrompt = 'enhance_image_prompt';
  static const String _keyImageCount = 'image_count';
  static const String _keyStudioHeaderCollapsed = 'studio_header_collapsed';
  static const String _keyChatHistory = 'chat_history_v1';
  static const int _maxHistoryItems = 30;

  Future<void> saveSettings({
    required String apiKey,
    String? imageApiKey,
    required String baseUrl,
    String? imageBaseUrl,
    required String chatModel,
    required String imageModel,
    String? imageEditModel,
    String imageAspectRatio = '1:1',
    String imageQuality = 'auto',
    bool enhanceImagePrompt = true,
    int imageCount = 1,
    bool? studioHeaderCollapsed,
  }) async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.setString(_keyApiKey, apiKey);
    await prefs.setString(_keyImageApiKey, (imageApiKey ?? '').trim());
    await prefs.setString(_keyBaseUrl, baseUrl);
    await prefs.setString(_keyImageBaseUrl, (imageBaseUrl ?? '').trim());
    await prefs.setString(_keyChatModel, chatModel);
    await prefs.setString(_keyImageModel, imageModel);
    await prefs.setString(_keyImageEditModel, (imageEditModel ?? '').trim());
    await prefs.setString(_keyImageAspectRatio, imageAspectRatio.trim().isEmpty ? '1:1' : imageAspectRatio.trim());
    await prefs.setString(_keyImageQuality, imageQuality.trim().isEmpty ? 'auto' : imageQuality.trim());
    await prefs.setBool(_keyEnhanceImagePrompt, enhanceImagePrompt);
    await prefs.setInt(_keyImageCount, imageCount.clamp(1, 4).toInt());
    if (studioHeaderCollapsed != null) await prefs.setBool(_keyStudioHeaderCollapsed, studioHeaderCollapsed);
  }

  Future<Map<String, String>> getSettings() async {
    final prefs = await SharedPreferences.getInstance();
    final baseUrl = prefs.getString(_keyBaseUrl) ?? '';
    final imageBaseUrl = prefs.getString(_keyImageBaseUrl) ?? '';
    final imageApiKey = prefs.getString(_keyImageApiKey) ?? '';
    return {
      'apiKey': prefs.getString(_keyApiKey) ?? '',
      // 为空表示图片工具令牌沿用聊天令牌，兼容老版本设置。
      'imageApiKey': imageApiKey,
      'baseUrl': baseUrl,
      // 为空表示图片工具接口沿用聊天接口，兼容老版本设置。
      'imageBaseUrl': imageBaseUrl,
      'chatModel': prefs.getString(_keyChatModel) ?? '',
      'imageModel': prefs.getString(_keyImageModel) ?? '',
      'imageEditModel': prefs.getString(_keyImageEditModel) ?? '',
      'imageAspectRatio': prefs.getString(_keyImageAspectRatio) ?? '1:1',
      'imageQuality': prefs.getString(_keyImageQuality) ?? 'auto',
      'enhanceImagePrompt': (prefs.getBool(_keyEnhanceImagePrompt) ?? true).toString(),
      'imageCount': (prefs.getInt(_keyImageCount) ?? 1).clamp(1, 4).toString(),
      'studioHeaderCollapsed': (prefs.getBool(_keyStudioHeaderCollapsed) ?? true).toString(),
    };
  }

  Future<List<ChatHistoryEntry>> getChatHistory() async {
    final prefs = await SharedPreferences.getInstance();
    final raw = prefs.getString(_keyChatHistory);
    if (raw == null || raw.trim().isEmpty) return const <ChatHistoryEntry>[];
    try {
      final decoded = jsonDecode(raw);
      if (decoded is! List) return const <ChatHistoryEntry>[];
      final items = decoded
          .whereType<Map>()
          .map((e) => ChatHistoryEntry.fromJson(Map<String, dynamic>.from(e)))
          .where((e) => e.messages.isNotEmpty)
          .toList()
        ..sort((a, b) => b.updatedAt.compareTo(a.updatedAt));
      return items.take(_maxHistoryItems).toList();
    } catch (_) {
      return const <ChatHistoryEntry>[];
    }
  }

  Future<void> upsertChatHistory(ChatHistoryEntry entry) async {
    if (entry.messages.isEmpty) return;
    final prefs = await SharedPreferences.getInstance();
    final items = await getChatHistory();
    final next = <ChatHistoryEntry>[
      entry,
      ...items.where((e) => e.id != entry.id),
    ]..sort((a, b) => b.updatedAt.compareTo(a.updatedAt));
    await prefs.setString(_keyChatHistory, jsonEncode(next.take(_maxHistoryItems).map((e) => e.toJson()).toList()));
  }

  Future<void> deleteChatHistory(String id) async {
    final prefs = await SharedPreferences.getInstance();
    final next = (await getChatHistory()).where((e) => e.id != id).toList();
    await prefs.setString(_keyChatHistory, jsonEncode(next.map((e) => e.toJson()).toList()));
  }

  Future<void> clearChatHistory() async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.remove(_keyChatHistory);
  }

  Future<void> clearSettings() async {
    final prefs = await SharedPreferences.getInstance();
    await prefs.remove(_keyApiKey);
    await prefs.remove(_keyImageApiKey);
    await prefs.remove(_keyBaseUrl);
    await prefs.remove(_keyImageBaseUrl);
    await prefs.remove(_keyChatModel);
    await prefs.remove(_keyImageModel);
    await prefs.remove(_keyImageEditModel);
    await prefs.remove(_keyImageAspectRatio);
    await prefs.remove(_keyImageQuality);
    await prefs.remove(_keyEnhanceImagePrompt);
    await prefs.remove(_keyImageCount);
    await prefs.remove(_keyStudioHeaderCollapsed);
  }
}
''', encoding='utf-8')

          p = Path('lib/screens/home_screen.dart')
          s = p.read_text(encoding='utf-8')
          if 'List<ChatHistoryEntry> _history = [];' not in s:
              s = s.replace("  final List<String> _attachedBase64Images = [];\n\n  bool _loading = false;", "  final List<String> _attachedBase64Images = [];\n  List<ChatHistoryEntry> _history = [];\n  String? _currentSessionId;\n\n  bool _loading = false;")
          s = s.replace("    _loadSettings();\n  }", "    _loadSettings();\n    _loadHistory();\n  }", 1)
          if 'Future<void> _loadHistory() async' not in s:
              anchor = "  Future<void> _saveSettings() => _settings.saveSettings(\n        apiKey: _apiKey,\n        imageApiKey: _imageApiKey,\n        baseUrl: _baseUrl,\n        imageBaseUrl: _imageBaseUrl,\n        chatModel: _chatModel,\n        imageModel: _imageModel,\n        imageEditModel: _imageEditModel,\n        imageAspectRatio: _imageAspectRatio,\n        imageQuality: _imageQuality,\n        enhanceImagePrompt: _enhanceImagePrompt,\n        imageCount: _imageCount,\n        studioHeaderCollapsed: _studioHeaderCollapsed,\n      );"
              insert = anchor + r'''

  Future<void> _loadHistory() async {
    final items = await _settings.getChatHistory();
    if (!mounted) return;
    setState(() => _history = items);
  }

  String _sessionTitle(List<Message> messages) {
    String? firstUser;
    for (final message in messages) {
      if (message.role == 'user' && message.content.trim().isNotEmpty) {
        firstUser = message.content.trim();
        break;
      }
    }
    final raw = firstUser ?? (messages.any((m) => m.type == MessageType.image) ? '图片创作会话' : '未命名会话');
    return raw.length > 22 ? '${raw.substring(0, 22)}…' : raw;
  }

  Future<void> _persistCurrentSession() async {
    if (_messages.isEmpty || _messages.any((m) => m.isGenerating)) return;
    final cleanMessages = _messages.where((m) => m.content.trim().isNotEmpty || m.type == MessageType.image || m.effectiveLocalFilePaths.isNotEmpty).toList();
    if (cleanMessages.isEmpty) return;
    final id = _currentSessionId ?? DateTime.now().microsecondsSinceEpoch.toString();
    _currentSessionId = id;
    await _settings.upsertChatHistory(ChatHistoryEntry(
      id: id,
      title: _sessionTitle(cleanMessages),
      updatedAt: DateTime.now(),
      messages: cleanMessages,
    ));
    await _loadHistory();
  }'''
              s = s.replace(anchor, insert)
          s = s.replace("当前聊天内容会从界面中清空，API 配置、图片参数和面板状态会保留。", "当前聊天会先保存到历史记录，然后从界面中清空；API 配置、图片参数和面板状态会保留。")
          s = s.replace("    if (confirmed == true) _startNewSession();", "    if (confirmed == true) {\n      await _persistCurrentSession();\n      _startNewSession();\n    }")
          s = s.replace("      _loading = false;\n      _forceImage = false;", "      _loading = false;\n      _forceImage = false;\n      _currentSessionId = null;", 1)
          s = s.replace("      setState(() {\n        _loading = false;\n        _forceImage = false;\n      });\n      _scrollBottom();", "      setState(() {\n        _loading = false;\n        _forceImage = false;\n      });\n      await _persistCurrentSession();\n      _scrollBottom();", 1)
          if "tooltip: '历史记录'" not in s:
              s = s.replace("        actions: [\n          IconButton(\n            tooltip: '新会话',", "        actions: [\n          IconButton(\n            tooltip: '历史记录',\n            onPressed: _openHistory,\n            icon: const Icon(Icons.history_rounded, color: _text),\n          ),\n          IconButton(\n            tooltip: '新会话',")
          if 'Future<void> _openHistory() async' not in s:
              history_method = r'''
  Future<void> _openHistory() async {
    await _persistCurrentSession();
    if (!mounted) return;
    await showModalBottomSheet<void>(
      context: context,
      isScrollControlled: true,
      backgroundColor: Colors.white,
      showDragHandle: true,
      builder: (ctx) => StatefulBuilder(builder: (ctx, sheetSetState) {
        Future<void> refresh() async {
          final items = await _settings.getChatHistory();
          if (!mounted) return;
          setState(() => _history = items);
          sheetSetState(() {});
        }

        return SafeArea(
          child: Padding(
            padding: EdgeInsets.fromLTRB(18, 8, 18, MediaQuery.of(ctx).padding.bottom + 18),
            child: SizedBox(
              height: MediaQuery.of(ctx).size.height * .72,
              child: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [
                Row(children: [
                  const Icon(Icons.history_rounded, color: _primary),
                  const SizedBox(width: 8),
                  const Expanded(child: Text('历史记录', style: TextStyle(color: _text, fontSize: 21, fontWeight: FontWeight.w900))),
                  if (_history.isNotEmpty)
                    TextButton(
                      onPressed: () async {
                        await _settings.clearChatHistory();
                        await refresh();
                        if (mounted) _snack('历史记录已清空');
                      },
                      child: const Text('清空'),
                    ),
                ]),
                const SizedBox(height: 10),
                Expanded(
                  child: _history.isEmpty
                      ? const Center(child: Text('暂无历史记录\n发送消息后会自动保存', textAlign: TextAlign.center, style: TextStyle(color: _muted, height: 1.5, fontWeight: FontWeight.w700)))
                      : ListView.separated(
                          itemCount: _history.length,
                          separatorBuilder: (_, __) => const SizedBox(height: 8),
                          itemBuilder: (_, i) {
                            final item = _history[i];
                            final time = '${item.updatedAt.month.toString().padLeft(2, '0')}-${item.updatedAt.day.toString().padLeft(2, '0')} ${item.updatedAt.hour.toString().padLeft(2, '0')}:${item.updatedAt.minute.toString().padLeft(2, '0')}';
                            return Material(
                              color: const Color(0xFFF7F4FF),
                              borderRadius: BorderRadius.circular(18),
                              child: ListTile(
                                shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(18)),
                                leading: Container(width: 42, height: 42, decoration: BoxDecoration(gradient: const LinearGradient(colors: [_primary, _primary2]), borderRadius: BorderRadius.circular(14)), child: const Icon(Icons.chat_bubble_rounded, color: Colors.white)),
                                title: Text(item.title, maxLines: 1, overflow: TextOverflow.ellipsis, style: const TextStyle(color: _text, fontWeight: FontWeight.w900)),
                                subtitle: Text('$time · ${item.messages.length} 条消息', style: const TextStyle(color: _muted, fontWeight: FontWeight.w700)),
                                onTap: () {
                                  setState(() {
                                    _currentSessionId = item.id;
                                    _messages
                                      ..clear()
                                      ..addAll(item.messages.map((m) => Message.fromJson(m.toJson())));
                                    _attachedFiles.clear();
                                    _attachedBase64Images.clear();
                                    _loading = false;
                                    _forceImage = false;
                                  });
                                  Navigator.pop(ctx);
                                  _scrollBottom();
                                  _snack('已恢复历史会话');
                                },
                                trailing: IconButton(
                                  tooltip: '删除',
                                  icon: const Icon(Icons.delete_outline_rounded, color: _muted),
                                  onPressed: () async {
                                    await _settings.deleteChatHistory(item.id);
                                    if (_currentSessionId == item.id && mounted) setState(() => _currentSessionId = null);
                                    await refresh();
                                  },
                                ),
                              ),
                            );
                          },
                        ),
                ),
              ]),
            ),
          ),
        );
      }),
    );
  }

'''
              s = s.replace('  Widget _studioHeader() {', history_method + '  Widget _studioHeader() {', 1)
          s = s.replace("            TextButton.icon(\n              onPressed: () => _setStudioHeaderCollapsed(true),\n              icon: const Icon(Icons.keyboard_arrow_up_rounded),\n              label: const Text('收起'),\n            ),", "            _roundPanelButton(\n              icon: Icons.keyboard_arrow_up_rounded,\n              tooltip: '收起创作面板',\n              onTap: () => _setStudioHeaderCollapsed(true),\n            ),")
          s = s.replace("            TextButton.icon(\n              onPressed: () => _setStudioHeaderCollapsed(false),\n              icon: const Icon(Icons.keyboard_arrow_down_rounded),\n              label: const Text('展开'),\n            ),", "            _roundPanelButton(\n              icon: Icons.keyboard_arrow_down_rounded,\n              tooltip: '展开创作面板',\n              onTap: () => _setStudioHeaderCollapsed(false),\n            ),")
          if 'Widget _roundPanelButton' not in s:
              round_method = r'''  Widget _roundPanelButton({required IconData icon, required String tooltip, required VoidCallback onTap}) => Tooltip(
        message: tooltip,
        child: InkWell(
          onTap: onTap,
          borderRadius: BorderRadius.circular(18),
          child: Container(
            width: 44,
            height: 44,
            decoration: BoxDecoration(
              gradient: const LinearGradient(colors: [_primary, _primary2]),
              borderRadius: BorderRadius.circular(18),
              boxShadow: [BoxShadow(color: _primary.withOpacity(.22), blurRadius: 14, offset: const Offset(0, 6))],
            ),
            child: Icon(icon, color: Colors.white, size: 28),
          ),
        ),
      );

'''
              s = s.replace('  Widget _modeButton', round_method + '  Widget _modeButton', 1)
          if "label: const Text('历史记录')" not in s:
              s = s.replace("              OutlinedButton.icon(\n                onPressed: () => _setStudioHeaderCollapsed(false),", "              OutlinedButton.icon(\n                onPressed: _openHistory,\n                icon: const Icon(Icons.history_rounded),\n                label: const Text('历史记录'),\n              ),\n              OutlinedButton.icon(\n                onPressed: () => _setStudioHeaderCollapsed(false),")
          p.write_text(s, encoding='utf-8')

      - name: Format and analyze changed Dart files
        run: |
          dart format lib/models/message.dart lib/services/settings_service.dart lib/screens/home_screen.dart
          flutter pub get
          flutter analyze

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add lib/models/message.dart lib/services/settings_service.dart lib/screens/home_screen.dart
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Add chat history and improve panel toggle UI"
            git push origin HEAD:${{ env.BRANCH_NAME }}
          fi
